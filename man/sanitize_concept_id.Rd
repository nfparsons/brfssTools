% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sanitize.R
\name{sanitize_concept_id}
\alias{sanitize_concept_id}
\title{Sanitize a variable name into an R-friendly concept_id}
\usage{
sanitize_concept_id(x, resolve_collisions = TRUE)
}
\arguments{
\item{x}{Character vector of raw variable names.}

\item{resolve_collisions}{Logical. If TRUE (default), and two raw names
sanitize to the same concept_id, append \verb{_2}, \verb{_3}, etc. to
disambiguate. If FALSE, returns whatever the rules produce (may have
duplicates).}
}
\value{
Character vector of sanitized concept_ids, same length as \code{x}.
}
\description{
Rules applied, in order:
\enumerate{
\item Strip a single leading underscore (CDC's \verb{_RACE} -> \code{RACE})
\item Replace any non-alphanumeric, non-underscore character with \verb{_}
\item Collapse runs of \verb{_} to a single \verb{_}
\item Strip trailing \verb{_}
\item If result starts with a digit, prepend \code{X}
\item Truncate to 63 characters (R's default name limit)
}
}
\examples{
\dontrun{
sanitize_concept_id(c("_RACE", "MENTHLTH", "X_ACE_1", "1AGE", "FOO BAR"))
# "RACE", "MENTHLTH", "X_ACE_1", "X1AGE", "FOO_BAR"

# Collision handling: both "_RACE" and "RACE" are present
sanitize_concept_id(c("_RACE", "RACE"))
# "RACE", "RACE_2"
}
}
