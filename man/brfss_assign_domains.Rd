% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxonomy.R
\name{brfss_assign_domains}
\alias{brfss_assign_domains}
\title{Auto-assign domain and subdomain for a vector of concept_ids}
\usage{
brfss_assign_domains(concept_ids, raw_var_names = NULL)
}
\arguments{
\item{concept_ids}{Character vector of concept_ids to assign.}

\item{raw_var_names}{Optional character vector of the un-sanitized
names (same length as \code{concept_ids}). Used as a fallback lookup if
the sanitized name doesn't match.}
}
\value{
A tibble with columns \code{concept_id}, \code{domain}, \code{subdomain}.
}
\description{
Looks up each concept_id (or its un-sanitized form) in the shipped
CDC codebook's \code{section_name} column. Matches are normalized through
a hand-curated dictionary (\code{.brfss_section_norm}) to a
subdomain, then the subdomain -> domain mapping from
\code{taxonomy.csv} is applied.
}
\details{
Concepts that don't match any CDC variable get
\code{domain = "Unassigned"}, \code{subdomain = NA}.
}
\examples{
\dontrun{
brfss_assign_domains(c("MENTHLTH", "POORHLTH", "ACEDEPRS",
                       "WEIRD_CUSTOM_VAR"))
# MENTHLTH        Behavioral Health  Healthy Days
# POORHLTH        Behavioral Health  Healthy Days
# ACEDEPRS        Behavioral Health  Adverse Childhood Experiences
# WEIRD_CUSTOM_VAR Unassigned         <NA>
}
}
