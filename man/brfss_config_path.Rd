% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paths.R
\name{brfss_config_path}
\alias{brfss_config_path}
\title{Resolve the brfssTools config path}
\usage{
brfss_config_path(path = NULL, must_exist = TRUE)
}
\arguments{
\item{path}{Optional explicit path. If supplied, takes priority.}

\item{must_exist}{Logical. If TRUE, error when resolved path doesn't exist.}
}
\value{
Character path.
}
\description{
Priority order (highest first):
\enumerate{
\item Explicit \code{path} argument
\item R option \code{brfssTools.config_path}
\item Environment variable \code{BRFSSTOOLS_CONFIG_PATH}
\item \code{tools::R_user_dir("brfssTools", which = "config")}
}
}
\details{
If \code{must_exist = TRUE} (default), errors when no candidate path exists
on disk. If \code{must_exist = FALSE}, returns the resolved path even if it
doesn't exist yet (useful for \code{brfss_init_state()}).

Use \code{package_extdata()} (separate function) for read-only access to the
package's shipped CDC codebook and templates.
}
