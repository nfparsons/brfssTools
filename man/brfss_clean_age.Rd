% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_age.R
\name{brfss_clean_age}
\alias{brfss_clean_age}
\title{Derive a standardized age grouping from BRFSS age variables}
\usage{
brfss_clean_age(
  df,
  scheme = c("5yr", "6group", "10yr", "65plus", "samhsa", "suicide", "custom"),
  breaks = NULL,
  labels = NULL,
  output_col = "age_grp"
)
}
\arguments{
\item{df}{A wide tibble produced by \code{\link[=brfss_pull]{brfss_pull()}}.}

\item{scheme}{One of:
\itemize{
\item \code{"5yr"} — 13 categories: 18-24, 25-29, ..., 75-79, 80+.
\item \code{"6group"} — 6 categories: 18-24, 25-34, 35-44, 45-54, 55-64, 65+.
\item \code{"10yr"} — 7 categories: 18-24, 25-34, ..., 65-74, 75+.
\item \code{"65plus"} — binary: 18-64 / 65+.
\item \code{"samhsa"} — 18-25, 26-34, 35-49, 50+ (NSDUH/SAMHSA convention,
requires continuous age).
\item \code{"suicide"} — 18-24, 25-44, 45-64, 65+ (suicide-surveillance
convention).
\item \code{"custom"} — user-supplied \code{breaks} and \code{labels} (requires
continuous age).
}}

\item{breaks}{For \code{scheme = "custom"} only. Numeric vector of break
points passed to \code{\link[base:cut]{base::cut()}} (left-closed, right-open intervals).}

\item{labels}{For \code{scheme = "custom"} only. Character vector of
category labels, length \code{length(breaks) - 1}.}

\item{output_col}{Name of the column to add to \code{df}. Default
\code{"age_grp"}. Call multiple times with different \code{output_col} values
to attach more than one grouping to the same dataset.}
}
\value{
The input tibble with one new factor column whose levels are
ordered according to the requested scheme.
}
\description{
BRFSS exposes age in several drift-prone forms across years
(continuous top-coded, 13-level five-year groups, six-level groups,
and a 65+ binary). \code{brfss_clean_age()} picks from a menu of common
groupings and uses whichever source column is available for each
row, in a waterfall: continuous if present, else 5-year groups,
else 6-group, else 65+ binary.

Operates on the wide tibble produced by \code{\link[=brfss_pull]{brfss_pull()}}. Looks for
columns named \code{age_continuous}, \code{age_5yr}, \code{age_6group}, \code{age_65plus}
(the canonical concept IDs). At least one usable source must exist
for the requested scheme.
}
\examples{
\dontrun{
demos <- brfss_pull(cw, c("age_continuous", "age_5yr"),
                    dataset = "OR", output = "wide")

demos |> brfss_clean_age(scheme = "5yr")
demos |> brfss_clean_age(scheme = "65plus", output_col = "is_65plus")

# Custom breaks for an analysis-specific cut
demos |> brfss_clean_age(
  scheme = "custom",
  breaks = c(18, 30, 50, 70, Inf),
  labels = c("18-29", "30-49", "50-69", "70+")
)
}
}
