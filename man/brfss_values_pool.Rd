% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inventory.R
\name{brfss_values_pool}
\alias{brfss_values_pool}
\title{Generate a raw-values inventory from a registered pool}
\usage{
brfss_values_pool(dataset, source = dataset, vars = NULL, max_unique = 50L)
}
\arguments{
\item{dataset}{The dataset whose pool to scan.}

\item{source}{Source tag to assign in the resulting \code{source} column.
Defaults to \code{dataset}.}

\item{vars}{Optional character vector of raw variable names to
restrict the scan to. Defaults to \code{NULL} (scan all variables).}

\item{max_unique}{Skip variables with more than this many unique
values (presumed continuous or free-text). Default 50.}
}
\value{
A tibble with columns matching \code{raw_values_brfss.csv}:
\code{source}, \code{year}, \code{raw_var_name}, \code{code}, \code{label}, \code{is_missing},
plus an extra \code{n} column with the observed frequency. \code{label} and
\code{is_missing} start as \code{NA} for the user to fill in.
}
\description{
Scans every file in a registered pool and reports the unique observed
values for each variable, with frequency counts. Useful for building
the value-level crosswalk (\code{raw_values_brfss.csv}).

This reads each file in full, which can be slow on large national
files. Use \code{\link[=brfss_inventory_pool]{brfss_inventory_pool()}} first if you only
need column names and types.

Variables with more than \code{max_unique} distinct values are skipped, on
the assumption that they are continuous or free-text and don't belong
in a value lookup table.
}
\examples{
\dontrun{
brfss_set_pool("WA", "/path/to/washington_brfss")
wa_vals <- brfss_values_pool("WA", vars = c("ASTHNOW", "_AGEG5YR"))
}
}
