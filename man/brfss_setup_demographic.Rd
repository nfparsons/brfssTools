% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demographics_setup.R
\name{brfss_setup_demographic}
\alias{brfss_setup_demographic}
\title{Set up a demographic transformation from a shipped template}
\usage{
brfss_setup_demographic(
  name,
  template = NULL,
  path = NULL,
  edit = interactive(),
  overwrite = FALSE
)
}
\arguments{
\item{name}{The demographic name, becomes the output column.
One of: \code{"age"}, \code{"sex"}, \code{"race"}, \code{"ethnicity"}, \code{"education"},
\code{"income"}. (Other names are accepted but won't have shipped
templates.)}

\item{template}{The template variant. See
\code{\link[=brfss_list_demographic_templates]{brfss_list_demographic_templates()}} for the full set. If NULL,
uses the package's default for that demographic.}

\item{path}{Optional config dir override.}

\item{edit}{Logical. If TRUE (default in interactive sessions),
opens the resulting YAML in the user's editor.}

\item{overwrite}{Logical. Default FALSE.}
}
\value{
The destination YAML path, invisibly.
}
\description{
Copies a built-in template YAML into your config dir's
\verb{transformations/} folder, ready to use with \code{brfss_pull()}. After
setup, edit the YAML if your codebook uses different column names
than the template assumes (most state codebooks need at least
light editing).
}
\details{
If a transformation with the same name already exists, this errors
unless \code{overwrite = TRUE}.
}
\examples{
\dontrun{
# Set up the standard CDC age groupings:
brfss_setup_demographic("age", template = "cdc_6group")

# Self-reported binary sex:
brfss_setup_demographic("sex", template = "selfreport_binary")

# CDC's 9-category race:
brfss_setup_demographic("race", template = "cdc_9category")
}
}
