% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosswalk_io.R
\name{cw_map_seeded}
\alias{cw_map_seeded}
\title{Map a seeded CDC variable to a state variable}
\usage{
cw_map_seeded(
  bundle,
  year,
  cdc_var,
  state_var,
  concept_id = NULL,
  source = "manual_edit",
  notes = NULL,
  allow_unknown = FALSE
)
}
\arguments{
\item{bundle}{Bundle from \code{cw_load()}.}

\item{year}{Integer year.}

\item{cdc_var}{CDC variable name (must exist as a seeded row).}

\item{state_var}{State variable name (must exist in state_codebook
unless \code{allow_unknown = TRUE}).}

\item{concept_id}{Optional new concept_id. If NULL, leaves it unchanged.}

\item{source}{Source label. Default "manual_edit" (replaces "cdc_seed").}

\item{notes}{Optional notes string.}

\item{allow_unknown}{If TRUE, won't error when state_var is missing
from the state codebook for this year.}
}
\value{
Updated bundle.
}
\description{
Workflow: a CDC-seeded crosswalk row has \code{state_var = NA} because
the user hasn't yet identified the state column for it. This function
updates the seeded row in place by setting its \code{state_var}, optionally
updating other fields (concept_id, source, notes), and clearing the
\code{unverified} flag.
}
\details{
If you're starting from an unseeded crosswalk (no row exists for this
CDC variable in this year), use \code{cw_add_pair()} instead.
}
\examples{
\dontrun{
cw <- cw_load()
# 2024's ASTHMA3 seeded row has state_var = NA; map it to your codebook:
cw <- cw_map_seeded(cw, year = 2024, cdc_var = "ASTHMA3",
                    state_var = "ASTHMA3")
cw_save(cw)
}
}
