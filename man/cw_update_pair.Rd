% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosswalk_io.R
\name{cw_update_pair}
\alias{cw_update_pair}
\title{Update fields on an existing pair}
\usage{
cw_update_pair(
  bundle,
  year,
  state_var,
  cdc_var,
  concept_id = NULL,
  is_primary = NULL,
  source = NULL,
  score = NULL,
  notes = NULL
)
}
\arguments{
\item{bundle}{Bundle from \code{cw_load()}.}

\item{year}{Integer year.}

\item{state_var}{State variable name (must exist in state_codebook,
unless \code{allow_unknown=TRUE}).}

\item{cdc_var}{CDC variable name (must exist in cdc_codebook).}

\item{concept_id}{The conceptual identifier this pair belongs to. Defaults
to \code{cdc_var}. Must be R-friendly: alphanumeric + underscore, no leading
digit.}

\item{is_primary}{1 if this is the canonical pair, 0 if secondary mapping.
When unset, becomes 1 unless an existing primary already exists for this
(year, state_var), in which case becomes 0.}

\item{source}{Free-text source label. Default 'manual_edit'.}

\item{score}{Optional confidence score; NA if hand-curated.}

\item{notes}{Optional notes string.}
}
\value{
Updated bundle.
}
\description{
Only \code{concept_id}, \code{is_primary}, \code{source}, \code{score}, and \code{notes} are
editable. To change \code{state_var} or \code{cdc_var}, remove the pair and add a
new one.
}
