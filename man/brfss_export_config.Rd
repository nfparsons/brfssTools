% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_io.R
\name{brfss_export_config}
\alias{brfss_export_config}
\title{Export the brfssTools config directory as a portable zip}
\usage{
brfss_export_config(zip_path = NULL, path = NULL)
}
\arguments{
\item{zip_path}{Where to write the zip file. Defaults to
\verb{brfssTools_config_<date>.zip} in the current working directory.}

\item{path}{Optional config dir override. Defaults to
\code{brfss_config_path()}.}
}
\value{
The zip file path, invisibly.
}
\description{
Bundles \code{crosswalk.csv}, \code{state_codebook.csv}, \code{state_only.csv},
\code{pending.csv}, \code{config.yaml}, and the entire \verb{transformations/}
subdirectory into a single zip file. Useful for moving work between
machines, archiving snapshots, or sharing a curated crosswalk with
collaborators.
}
\details{
Things NOT included:
\itemize{
\item \code{.bak} backup files (kept locally only)
\item Anything in the package's \verb{inst/extdata/} (the CDC codebook,
taxonomy — these come from the package install on the destination
machine, not from this archive)
\item Cached data files (see \code{\link[=brfss_cache_dir]{brfss_cache_dir()}} for those)
}
}
\examples{
\dontrun{
# Snapshot current config before a major refactor:
brfss_export_config("~/snapshots/brfss_2026-04-30.zip")

# Move work to a new machine:
brfss_export_config("/share/oregon_crosswalk_v1.zip")
# ... copy the zip over, then on the new machine:
brfss_import_config("/share/oregon_crosswalk_v1.zip")
}
}
