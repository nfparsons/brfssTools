% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demographics.R
\name{brfss_clean_race}
\alias{brfss_clean_race}
\title{Derive Standardized Race/Ethnicity from BRFSS Data}
\usage{
brfss_clean_race(df, standard = c("fewest", "cdc", "reald"))
}
\arguments{
\item{df}{A wide dataframe produced by \code{\link[=brfss_pull]{brfss_pull()}}.}

\item{standard}{String: \code{"fewest"}, \code{"cdc"}, or \code{"reald"}.
\itemize{
\item \code{"fewest"} produces a single column \code{race_eth_fewest} with three
levels: Hispanic/Latino, White Non-Hispanic, BIPOC/Global Majority.
\item \code{"cdc"} produces a single column \code{race_eth_cdc} with eight
mutually-exclusive levels.
\item \code{"reald"} produces a set of \verb{reald_*} binary indicator columns
for each Asian and Pacific Islander subgroup separable in MRACE.
}}
}
\value{
The input dataframe with the requested column(s) appended.
}
\description{
Operates on a wide tibble produced by \code{\link[=brfss_pull]{brfss_pull()}} containing
\verb{race_array_*} and (optionally) \verb{hisp_array_*} columns. The function
interprets race-array values as \strong{MRACE codes} — the BRFSS standard
collapsed encoding that ships in OR's analyst-facing files and CDC's
submitted records. MRACE codes are:
\itemize{
\item 10 White (also receives REX MENA codes 80-82 via CDC's collapse)
\item 20 Black or African American
\item 30 American Indian or Alaska Native
\item 40 Asian umbrella, 41-47 Asian subgroups
(41 Asian Indian, 42 Chinese, 43 Filipino, 44 Japanese,
45 Korean, 46 Vietnamese, 47 Other Asian)
\item 50 PI umbrella, 51-54 PI subgroups
(51 Native Hawaiian, 52 Guamanian/Chamorro,
53 Samoan, 54 Other Pacific Islander)
\item 60 Other, 88 No additional choices, 99 Refused
}

Hispanic-array values are interpreted as HISPANC3 codes
(1 Mexican, 2 Puerto Rican, 3 Cuban, 4 Other Hispanic, 5 No,
7 DK, 9 Refused). Hispanic detection scans all \verb{hisp_array_*} slots
for any code 1-4.
}
\section{Granularity caveat}{

MRACE encoding is the maximum granularity available in standard CDC
BRFSS files. Categories collapsed by CDC's REX-to-MRACE derivation
(Slavic, Eastern European, Afro-Caribbean, Ethiopian, Somali, Hmong,
Marshallese, Middle Eastern, North African) are not separable here.
Recovering them requires the raw REX1-REX10 columns, which are not
typically distributed with analyst-facing files.
}

\examples{
\dontrun{
demos <- brfss_pull(
  cw,
  c(paste0("race_array_", 1:14), paste0("hisp_array_", 1:4)),
  dataset = "OR", output = "wide"
)
demos |> brfss_clean_race(standard = "cdc")
demos |> brfss_clean_race(standard = "fewest")
demos |> brfss_clean_race(standard = "reald")
}
}
