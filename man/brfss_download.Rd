% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{brfss_download}
\alias{brfss_download}
\title{Download national CDC BRFSS LLCP files}
\usage{
brfss_download(year, dir = brfss_cache_dir(), overwrite = FALSE, quiet = FALSE)
}
\arguments{
\item{year}{Integer scalar or vector of survey years to download.
All must be \verb{>= 2011}.}

\item{dir}{Cache directory. Defaults to \code{\link[=brfss_cache_dir]{brfss_cache_dir()}}.}

\item{overwrite}{If \code{TRUE}, re-download files even if a local copy exists.
Default \code{FALSE}.}

\item{quiet}{If \code{TRUE}, suppress progress messages from the underlying
download. Default \code{FALSE}.}
}
\value{
Invisibly, a tibble with one row per requested year and columns
\code{year}, \code{xpt_path}, \code{status} (\code{"downloaded"}, \code{"cached"}, or \code{"failed"}).
}
\description{
Downloads the public combined landline + cell phone BRFSS data files
(\verb{LLCP\{YYYY\}.XPT}) for the requested years, extracts them into a local
cache, and registers the cache as the \code{"National"} pool. After this runs,
you can immediately call \code{brfss_pull(..., dataset = "National")}.

Only years 2011 and later are supported; the 2011 weighting methodology
change (raking + cell phone frame) makes earlier files non-comparable.
}
\examples{
\dontrun{
# First-time setup, pull a few years of national data
brfss_download(2020:2023)

# Now usable like any other dataset
cw <- brfss_crosswalk()
brfss_pull(cw, c("survey_weight", "fmd"),
           dataset = "National", states = c("OR", "WA"))
}
}
