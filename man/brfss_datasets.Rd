% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\name{brfss_datasets}
\alias{brfss_datasets}
\title{List datasets known to brfssTools}
\usage{
brfss_datasets()
}
\value{
A tibble with columns \code{dataset}, \code{description}, \code{format},
\code{download_supported}, and \code{state_filter_supported}. The current
pool-registration status is added by \code{\link[=brfss_pool_status]{brfss_pool_status()}}.
}
\description{
Returns a tibble describing every dataset the package knows about,
along with metadata indicating which features are available
(\code{brfss_download()} support, state-level filtering, etc.). The
\code{dataset} column lists the canonical identifiers that
\code{brfss_pull(dataset = ...)}, \code{brfss_set_pool(dataset, ...)}, and
\code{brfss_crosswalk(dataset = ...)} accept.
}
\details{
To add a new dataset to the registry, edit
\code{.brfss_dataset_registry()} in \code{R/datasets.R}. No other code changes
are required — \code{brfss_pull()} and friends pick up new datasets
automatically.
}
\examples{
brfss_datasets()
}
