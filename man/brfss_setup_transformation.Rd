% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transformations.R
\name{brfss_setup_transformation}
\alias{brfss_setup_transformation}
\title{Open or create a transformation R file in the user's editor}
\usage{
brfss_setup_transformation(
  name,
  template = NULL,
  path = NULL,
  edit = interactive()
)
}
\arguments{
\item{name}{Character, the transformation name. Becomes the output column
name. Must be R-friendly (alphanumeric + underscore, no leading digit).}

\item{template}{Optional name of a built-in template. Currently \code{"race"}
is supplied; everything else gets a generic stub.}

\item{path}{Optional config path override.}

\item{edit}{Logical. If TRUE (default in interactive sessions), opens the
file in the user's editor via \code{utils::file.edit()}. If FALSE, returns
the path silently.}
}
\value{
The transformation file path, invisibly.
}
\description{
Transformation files live in \verb{<config>/transformations/<name>.R}. The file
must define a single function with signature \verb{function(.data, year)} that
returns a tibble with the new column added. The file name (without \code{.R})
becomes the column name \code{brfss_pull()} produces.
}
\details{
If the file already exists, this opens it for editing. If not, this
writes a starter template (parameterized by \code{template}) and opens that.
}
