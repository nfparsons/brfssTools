% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey.R
\name{brfss_as_svy}
\alias{brfss_as_svy}
\title{Convert a wide BRFSS pull result into a survey design}
\usage{
brfss_as_svy(
  data,
  weights = "survey_weight",
  ids = "psu",
  strata = "strata",
  nest = TRUE
)
}
\arguments{
\item{data}{A wide tibble produced by \code{\link[=brfss_pull]{brfss_pull()}} with
\code{output = "wide"}. Must contain a numeric-coercible weight column.}

\item{weights}{Name of the weight column. Default \code{"survey_weight"}.}

\item{ids}{Name of the PSU / cluster column. Default \code{"psu"}. If the
column is missing, the design is built without a clustering
argument (srvyr defaults to one cluster per row).}

\item{strata}{Name of the stratum column. Default \code{"strata"}. If the
column is missing or all-NA, the design is built without
stratification.}

\item{nest}{Passed through to \code{\link[srvyr:as_survey_design]{srvyr::as_survey_design()}}. Default
\code{TRUE}, the right choice when PSUs are nested within strata
(BRFSS national). Forced to \code{FALSE} if strata are absent.}
}
\value{
A \code{tbl_svy} object from the \code{srvyr} package.
}
\description{
Wraps a wide-format tibble produced by \code{\link[=brfss_pull]{brfss_pull()}} in a
\code{\link[srvyr:as_survey_design]{srvyr::as_survey_design()}} object, ready for design-aware estimation.

Handles two real-world quirks:
\itemize{
\item \strong{Strata may be absent.} Oregon BRFSS files do not carry a stratum
variable for most years. If the \code{strata} column is missing entirely,
or is present but contains only \code{NA}, this function falls back to a
single-stratum design (no \code{strata} argument passed to srvyr).
\item \strong{Harmonized values are character.} The recode engine returns
character vectors (because recode rules can map to non-numeric
labels). For the design columns we coerce: \code{survey_weight} becomes
numeric. \code{psu} and \code{strata} stay as-is — character/factor values
are valid stratum and PSU identifiers.
}

Rows with \code{NA} or non-positive weights are dropped, since survey
design objects require positive weights.
}
\examples{
\dontrun{
library(srvyr)

or_data <- brfss_pull(
  cw, c("ASTHNOW", "GENHLTH"),
  dataset = "OR", output = "wide"
)

or_svy <- brfss_as_svy(or_data)

or_svy |>
  group_by(GENHLTH) |>
  summarize(prev = survey_mean(ASTHNOW == "1", na.rm = TRUE,
                               vartype = "ci"))
}
}
\seealso{
\code{\link[=brfss_pull]{brfss_pull()}} to produce the input.
}
