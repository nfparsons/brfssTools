% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/categorical_map.R
\name{brfss_setup_categorical_map}
\alias{brfss_setup_categorical_map}
\title{Set up a declarative categorical-map transformation}
\usage{
brfss_setup_categorical_map(
  name,
  template = NULL,
  path = NULL,
  edit = interactive()
)
}
\arguments{
\item{name}{The transformation name. Becomes the output column name.}

\item{template}{Optional template name. \code{"race"} is supplied; everything
else gets a generic stub.}

\item{path}{Optional config path override.}

\item{edit}{Logical; opens the file in editor when interactive.}
}
\value{
The YAML file path, invisibly.
}
\description{
Creates \verb{<config>/transformations/<name>.yaml} with a starter template and
opens it for editing. The YAML file is the source of truth; the runtime
reads it on every \code{brfss_pull()} call.
}
\details{
Optionally, \code{brfss_render_transformation_code()} can generate a
human-readable R companion file (\verb{<name>.R}) that mirrors the YAML logic.
Useful for documentation, methods sections, or audit purposes.
}
