% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_national.R
\name{brfss_use_national}
\alias{brfss_use_national}
\title{Use CDC's public National BRFSS data}
\usage{
brfss_use_national(
  years = NULL,
  data_path = NULL,
  codebook_path = NULL,
  crosswalk = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{years}{Integer vector of years to download. Defaults to
\code{2018:current_year}.}

\item{data_path}{Optional override for where to store the downloaded
files. If NULL (default), uses the package cache directory.}

\item{codebook_path}{Optional override; defaults to
\code{<data_path>/documentation/}.}

\item{crosswalk}{Optional override for the crosswalk path; defaults
to \code{<codebook_path>/National_crosswalk.csv}.}

\item{overwrite}{Logical. If TRUE, re-download files even if cached.}
}
\value{
Invisibly, the resolved pool config.
}
\description{
Convenience function for users who want to work with CDC's public
BRFSS files. Downloads the requested years' LLCP \code{.xpt} files to a
local cache, then registers them as a pool with sensible defaults.
}
\details{
For a National-data user, the typical flow is:
\preformatted{
  brfss_use_national(years = 2018:2024)
  brfss_draft_crosswalk(dataset = "National")
  brfss_crosswalk_editor(dataset = "National")
}

(A pre-built National crosswalk is planned for a future release; once
shipped, the draft step will be optional.)

Files cache to \code{tools::R_user_dir("brfssTools", "cache")} by
default. Subsequent runs are fast — already-downloaded files are
reused.
}
