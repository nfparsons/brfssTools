% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state_tag_audit.R
\name{brfss_state_tag_audit}
\alias{brfss_state_tag_audit}
\title{Audit \code{core}-tagged crosswalk rules against a reference dataset}
\usage{
brfss_state_tag_audit(
  cw,
  reference_dataset = "National",
  current_source = "core",
  suggested_source = "OR",
  strict_year_match = TRUE,
  ignore_case = FALSE
)
}
\arguments{
\item{cw}{Crosswalk object from \code{\link[=brfss_crosswalk]{brfss_crosswalk()}}. To audit
all \code{core} rows, pass a crosswalk that wasn't filtered by \code{dataset}.}

\item{reference_dataset}{Name of the registered pool to use as the
reference. Default \code{"National"}. Must be registered via
\code{\link[=brfss_set_pool]{brfss_set_pool()}} or \code{\link[=brfss_download]{brfss_download()}}.}

\item{current_source}{Source tag to audit. Default \code{"core"}.}

\item{suggested_source}{Source tag to suggest for rows that fail
the inventory check. Default \code{"OR"}.}

\item{strict_year_match}{Logical. If \code{TRUE} (default), match
\code{raw_var_name} per-year against reference. If \code{FALSE}, match
across all years.}

\item{ignore_case}{Logical. If \code{TRUE}, match \code{raw_var_name} case-
insensitively. Default \code{FALSE} (BRFSS XPT files use stable
uppercase, so a case mismatch is a real curation issue worth
surfacing).}
}
\value{
A tibble with one row per audited \code{concept_map} row. Columns:
\code{concept_id}, \code{year}, \code{raw_var_name}, \code{source}, \code{in_reference},
\code{reference_years_seen} (comma-separated list, or \code{NA}),
\code{suggested_source}, \code{needs_action} (\code{TRUE} iff
\code{source != suggested_source}).
}
\description{
Compares each \code{source = "core"} row in the crosswalk's \code{concept_map}
against the actual variable inventory of a reference dataset (typically
\code{"National"}). Rows whose \code{raw_var_name} does not appear in the
reference inventory for that year are flagged as candidates for
retagging to a state-specific source.

Use this once when onboarding a National pool to clean up legacy
state-as-core curation noise. Inspect the returned tibble, decide
which rows to retag, then write a single migration script to apply
the changes.
}
\section{What "in reference" means}{

By default (\code{strict_year_match = TRUE}), a row counts as "present in
reference" only if its \verb{(year, raw_var_name)} pair appears in the
reference inventory at that exact year. Set
\code{strict_year_match = FALSE} to relax: a row counts as present if its
\code{raw_var_name} appears in the reference inventory in \strong{any} year.
Strict matching is more useful for actual cleanup; relaxed matching is
useful for sanity-checking whether a variable is "ever" in the
reference at all.
}

\examples{
\dontrun{
brfss_download(2012:2024)
cw <- brfss_crosswalk()  # no dataset filter -> all rules

audit <- brfss_state_tag_audit(cw)

# Inspect candidates for retagging
library(dplyr)
audit |>
  filter(needs_action) |>
  count(raw_var_name, suggested_source, sort = TRUE)

# Or look at a specific concept's audit verdict across years
audit |> filter(concept_id == "wtrk_all")
}
}
