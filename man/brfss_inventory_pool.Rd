% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inventory.R
\name{brfss_inventory_pool}
\alias{brfss_inventory_pool}
\title{Generate a raw-variable inventory from a registered pool}
\usage{
brfss_inventory_pool(dataset, source = dataset, n_max = 1000L)
}
\arguments{
\item{dataset}{The dataset whose pool to scan. Must already be
registered via \code{\link[=brfss_set_pool]{brfss_set_pool()}} or
\code{\link[=brfss_download]{brfss_download()}}.}

\item{source}{Source tag to assign in the resulting \code{source} column.
Defaults to \code{dataset} itself ("this variable was observed in the
\verb{<dataset>} files"). Override to \code{"core"} if you intend to merge
the result into the shared core crosswalk.}

\item{n_max}{Number of rows to read from each file. Default 1000.
Use a smaller value (e.g., 0 for headers only) if you trust
metadata.}
}
\value{
A tibble with columns matching \code{raw_inventory_brfss.csv}:
\code{source}, \code{year}, \code{raw_var_name}, \code{position}, \code{var_kind},
\code{question_text}, \code{module}, \code{missing_values_raw}, \code{source_file},
\code{parse_notes}. The latter four start as \code{NA} for the user to fill
in.
}
\description{
Scans every file in a registered pool and reports the variable names,
positions, types, and (for XPT files) any embedded SAS labels. The
returned tibble has the same shape as the bundled
\code{raw_inventory_brfss.csv} — useful as a starting point for adding a
new dataset to the master crosswalk, or for auditing what's actually
in your data files.

Type inference uses a small sample of rows by default (1000), which
is plenty since XPT files carry full type metadata and CSVs are
guessed by \code{readr}.
}
\examples{
\dontrun{
brfss_set_pool("WA", "/path/to/washington_brfss")
wa_inv <- brfss_inventory_pool("WA")

# Compare against current crosswalk to find new variables
cw <- brfss_crosswalk()
new_vars <- dplyr::anti_join(wa_inv, cw$inventory,
                             by = c("year", "raw_var_name"))
}
}
